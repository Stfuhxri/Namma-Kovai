{
  "rules": {
    "/liveLocationReports/$busId/$reportId": {
      ".write": "auth !== null && newData.child('userId').val() === auth.uid",
      ".validate": "newData.child('lat').val() >= 10.8 && newData.child('lat').val() <= 11.3 && newData.child('lng').val() >= 76.7 && newData.child('lng').val() <= 77.2 && newData.child('timestamp').val() <= now && newData.child('accuracy').val() >= 0 && newData.child('accuracy').val() <= 1000"
    },
    "/buses/$busId": {
      ".read": "true",
      ".write": "false"
    }
  }
}
